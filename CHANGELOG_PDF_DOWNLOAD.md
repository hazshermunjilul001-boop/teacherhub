# Exact SF9 PDF Download

The SF9 `Download This` action now exports the visible printable SF9 preview as an A4 landscape PDF. The export uses the same rendered preview, excludes TeacherHub-only non-print content, and preserves the one-page SF9 layout for Grades 2–10.

No grade-level routing, Grade 2/3 layout, Grade 4–10 layout, attendance aggregation, manual-grade clearing, or manual-grade entry behavior was changed in this update.

The existing bulk download action remains DOCX-based; the individual report download is now labeled `Download PDF` for exact print-preview fidelity.

Verification: `npm run build` completed successfully with TypeScript validation and route generation.
